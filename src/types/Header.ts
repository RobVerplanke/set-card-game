type NavButtonProps = {
  label: string;
  onClick: () => void;
};

type GameTitle = {
  label: string;
};

type PlayerName = {
  label: string;
};

export type { NavButtonProps, GameTitle, PlayerName };
